#一个整数，它加上100后是一个完全平方数，再加上168又是一个完全平方数，请问该数是多少？#
备注： **代表乘方 ==是判断； range()函数内只有一个参数，则表示会产生从0开始计数的整数列表 --- 左闭右开

1. 如何在list里添加元素？
A: a.append(x)
2. 如何表达某数的range？
A: for m in range (168):
3. 一定要注意t的缩写地方 要在所有if语句之后 但是也不能顶格写 否则if就没了







t = []
for m in range (168):
    for n in range (168):
        if m**2 - n**2 == 168:
            x = n**2 - 100
            t.append(x)
print(t)

