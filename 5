“输入三个整数x,y,z，请把这三个数由小到大输出”


a = [3, 4, 1]
result = []

for i in range(len(a)):
    min = a[i]
    for j in range(i+1,len(a)):
        if min > a[j]:
            temp = min
            min = a[j]
            a[j] = temp
    result.append(min)
print(result)
    
  
